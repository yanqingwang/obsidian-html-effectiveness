{
	"name": "obsidian-html-effectiveness",
	"version": "1.0.0",
	"description": "Render spatial HTML content (compare/diagram/slides/report/timeline) in Obsidian",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"esbuild": "^0.19.0",
		"obsidian": "^1.5.0",
		"tslib": "^2.6.0",
		"typescript": "^5.3.0"
	}
}
